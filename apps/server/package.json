{
  "name": "@open-watch-party/server",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "release": "cd ../.. && release-it --config .release-it.server.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@open-watch-party/shared": "workspace:*",
    "limiter": "^3.0.0",
    "lru-cache": "^11.3.5",
    "pino": "catalog:",
    "socket.io": "^4.8.3",
    "ts-pattern": "catalog:",
    "zod": "catalog:"
  }
}
