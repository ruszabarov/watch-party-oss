services:
  open-watch-party-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8787:8787'
    environment:
      PORT: '8787'
      ROOM_IDLE_TTL_MS: '21600000'
      ROOM_SWEEP_INTERVAL_MS: '300000'
      MAX_ROOMS: '1000'
