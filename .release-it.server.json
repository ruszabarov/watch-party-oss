{
  "$schema": "https://unpkg.com/release-it@20/schema/release-it.json",
  "git": {
    "requireBranch": "main",
    "commitMessage": "Release server v${version}",
    "tagName": "server-v${version}",
    "tagMatch": "server-v*",
    "tagAnnotation": "Release server v${version}"
  },
  "npm": false,
  "github": {
    "release": false
  },
  "hooks": {
    "before:init": [
      "pnpm lint",
      "pnpm format:check",
      "pnpm --filter @open-watch-party/shared test",
      "pnpm --filter @open-watch-party/server test"
    ]
  },
  "plugins": {
    "@release-it/bumper": {
      "in": {
        "file": "apps/server/package.json",
        "path": "version"
      },
      "out": {
        "file": "apps/server/package.json",
        "path": "version"
      }
    }
  }
}
